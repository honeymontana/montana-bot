# 📝 Конфигурация текстов сообщений бота

## Описание

Все тексты сообщений бота вынесены в файл `messages.json` для удобного редактирования без изменения кода.

## Структура файла

```json
{
  "welcome": {        // Приветственное сообщение (/start)
    "title": "...",
    "description": "..."
  },
  "help": {           // Справка (/help)
    "title": "...",
    "user_commands": { ... }
  },
  ...
}
```

## Как редактировать тексты

### 1. Простая замена текста

Откройте `messages.json` и найдите нужный текст:

```json
{
  "welcome": {
    "title": "👋 Добро пожаловать в Montana Helper Bot!"
  }
}
```

Измените на:

```json
{
  "welcome": {
    "title": "🎉 Привет! Добро пожаловать в Montana Bot!"
  }
}
```

### 2. Использование параметров

Некоторые тексты содержат параметры в фигурных скобках `{param}`:

```json
{
  "mystats": {
    "user_id": "• User ID: `{userId}`"
  }
}
```

**НЕ УДАЛЯЙТЕ** параметры `{userId}`, `{username}`, `{count}` и т.д. - они автоматически заменяются на реальные значения!

### 3. Эмодзи

Вы можете свободно добавлять/удалять эмодзи:

```json
{
  "status": {
    "member": "✅ Вы являетесь участником Montana"
  }
}
```

Можно изменить на:

```json
{
  "status": {
    "member": "🎊✅ Вы участник Montana! 🎉"
  }
}
```

### 4. Многострочные тексты

Используйте `\n` для переноса строки:

```json
{
  "addgroup": {
    "usage": "Использование: /addgroup <chat_id> [часы]\n\nПример:\n/addgroup -1001234567890 24"
  }
}
```

## Разделы конфигурации

### `welcome` - Приветствие (/start)
Сообщение, которое видит пользователь при первом запуске бота.

### `help` - Справка (/help)
Список всех доступных команд. Автоматически адаптируется:
- Для обычных пользователей - только базовые команды
- Для админов - показывает дополнительные админ-команды
- Если Discord включен - показывает Discord команды

### `status` - Статус (/status)
Информация о членстве пользователя в основной группе.

### `mystats` - Статистика (/mystats)
Персональная статистика пользователя:
- User ID
- Username
- Группы пользователя

**Доступные параметры:**
- `{userId}` - ID пользователя
- `{username}` - Username пользователя
- `{count}` - Количество групп

### `listgroups` - Список групп (/listgroups)
Список всех групп в системе (только для админов).

**Доступные параметры:**
- `{count}` - Количество групп
- `{chatId}` - ID группы
- `{hours}` - Часы доступа

### `sync` - Синхронизация (/sync)
Сообщения при синхронизации членства.

**Доступные параметры:**
- `{count}` - Количество удалённых пользователей

### `addgroup` - Добавление группы
Сообщения при добавлении обычной группы.

**Доступные параметры:**
- `{title}` - Название группы
- `{hours}` - Часы доступа

### `addpermanentgroup` - Добавление постоянной группы
Сообщения при добавлении постоянной группы (с пожизненным доступом).

**Доступные параметры:**
- `{title}` - Название группы
- `{hours}` - Часы доступа

### `removegroup` - Удаление группы
Сообщения при деактивации группы.

### `updategroup` - Обновление группы
Сообщения при обновлении настроек группы.

**Доступные параметры:**
- `{title}` - Название группы
- `{hours}` - Часы доступа
- `{createdAt}` - Дата создания

### `syncgroup` - Синхронизация группы
Сообщения при синхронизации участников группы.

**Доступные параметры:**
- `{synced}` - Количество синхронизированных
- `{errors}` - Количество ошибок

### `fullsync` - Полная синхронизация
Сообщения при полной синхронизации через MTProto API.

**Доступные параметры:**
- `{synced}` - Количество синхронизированных
- `{errors}` - Количество ошибок

### `discord` - Discord интеграция
Все сообщения связанные с Discord.

**Доступные параметры:**
- `{username}` - Discord username
- `{url}` - OAuth URL
- `{discordId}` - Discord user ID
- `{telegramUsername}` - Telegram username
- `{linkedAt}` - Дата привязки
- `{roleStatus}` - Статус роли

### `errors` - Ошибки
Общие сообщения об ошибках.

**Доступные параметры:**
- `{error}` - Текст ошибки

### `notifications` - Уведомления
Автоматические уведомления от бота.

**Доступные параметры:**
- `{message}` - Текст сообщения
- `{groupTitle}` - Название группы

## Примеры использования

### Изменить приветствие

**До:**
```json
{
  "welcome": {
    "title": "👋 Добро пожаловать в Montana Helper Bot!"
  }
}
```

**После:**
```json
{
  "welcome": {
    "title": "🎉 Привет! Это Montana Access Bot!"
  }
}
```

### Изменить текст ошибки

**До:**
```json
{
  "errors": {
    "unauthorized": "У вас нет прав для выполнения этой команды."
  }
}
```

**После:**
```json
{
  "errors": {
    "unauthorized": "⛔ Недостаточно прав! Эта команда доступна только админам."
  }
}
```

### Добавить больше информации в /help

Отредактируйте секцию `help.user_commands`:

```json
{
  "help": {
    "user_commands": {
      "start": "🏠 /start - Начать работу с ботом и увидеть приветствие",
      "status": "📊 /status - Узнать свой статус подписки Montana",
      "mystats": "📈 /mystats - Посмотреть свою статистику и группы",
      "help": "❓ /help - Показать эту справку со всеми командами"
    }
  }
}
```

## После редактирования

1. **Сохраните файл** `messages.json`
2. **Перезапустите бота**:
   ```bash
   npm run dev
   ```
   или
   ```bash
   npm run build
   npm start
   ```

3. **Проверьте изменения** отправив команду боту

## Важные замечания

⚠️ **Сохраняйте структуру JSON**
- Не забывайте запятые между элементами
- Проверяйте, что все кавычки закрыты
- Используйте валидатор JSON если не уверены

⚠️ **Не удаляйте параметры**
- Параметры вида `{userId}`, `{count}` нужны для работы
- Можно менять текст вокруг них

⚠️ **Используйте `\n` для переносов**
- Многострочные тексты требуют `\n`
- Не используйте реальные переносы внутри строк JSON

## Валидация JSON

Проверить корректность JSON можно онлайн:
- https://jsonlint.com/
- https://jsonformatter.org/

Или через команду:
```bash
node -e "JSON.parse(require('fs').readFileSync('config/messages.json', 'utf-8'))"
```

Если нет ошибок - всё в порядке! ✅

## Восстановление после ошибок

Если случайно сломали JSON:

1. Остановите бота
2. Восстановите из git:
   ```bash
   git checkout config/messages.json
   ```
3. Внесите изменения заново аккуратно
4. Запустите бота

---

**💡 Совет:** Делайте бэкапы перед большими изменениями!

```bash
cp config/messages.json config/messages.json.backup
```
